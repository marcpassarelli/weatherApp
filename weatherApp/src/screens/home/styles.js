import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  textInsertLocation: {
    marginTop: 50,
    alignSelf: 'center',
    fontSize: 20,
    fontWeight: 'bold',
  },
  cityName: {
    marginVertical: 20,
    fontSize: 20,
  },
})

export default styles
