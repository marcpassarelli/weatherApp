import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  textInsertLocation: {
    marginTop: 50,
    alignSelf: "center",
    fontSize: 20,
    fontWeight: "bold",
  },
});

export default styles;
